# Роли агентов и скиллы

## Общий контекст (обязателен для всех агентов)

**Импортозамещение и нормы РФ.** Все агенты должны учитывать скилл **`ru-compliance-context`**:

- Структура требований по импортозамещению в РФ (законы, подзаконные акты, отрасль, политики заказчика).
- **Система не является объектом ЗоКИИ** (не 187-ФЗ) — к ней не применяются режимы и ограничения КИИ.
- Допущения и легальные способы использования иностранного ПО и оборудования; не предполагать по умолчанию полное импортозамещение или нормы КИИ.
- Итоговые юридические выводы — за юристом или ответственным по комплаенсу.

Скилл: `.cursor/skills/ru-compliance-context/SKILL.md`

---

## Обязательный пайплайн разработки

Любая задача с изменением кода, API, БД или инфраструктуры проходит этапы **строго по порядку**. Полное описание этапов (Gatekeeper → аналитика → ИБ → тест-план → разработка → тестирование → проверка с Павлом → деплой → разбор на знания): **`.cursor/skills/tasktime-workflow/SKILL.md`**.

**Коммиты и push изменений, связанных с фичами, без явного подтверждения пользователя — запрещены.**  
**Деплой без явного подтверждения пользователя — запрещён.** Этап «Разбор на знания» обязателен.

---

## Роли и скиллы

| Роль | Скилл | Когда привлекать |
|------|--------|-------------------|
| **Разработчик** | `developer` | Реализация фич, рефакторинг, выбор технологий и библиотек. |
| **Тестировщик (авто + ручные)** | `tester` | Автотесты (unit, API, e2e), ручные сценарии, приёмочное тестирование. |
| **Специалист по информ. безопасности** | `infosec` | ИБ-меры, разграничение доступа, защита данных, комплаенс по ИБ. |
| **Deploy TaskTime** | `deploy-tasktime` | Деплой приложения на Ubuntu/Timeweb и целевые окружения (Ubuntu/Astra/RedOS). |
| **Процесс / workflow** | `tasktime-workflow` | Практический пайплайн разработки и деплоя для MVP (без жёсткой зависимости от gatekeeper MCP). |

---

## Интерфейс для ИИ-команды и визуализация

- **TaskTime** — только для **пользователей** (люди, задачи, учёт времени). Для ИИ-команды не используется.
- **Постановка задач ИИ-команде:** нужен **отдельный интерфейс** (очередь задач для агентов, выбор роли/скилла, с телефона или из браузера). В проекте пока не реализован — см. **docs/PIXEL_AGENTS_AND_TASKS.md** и техдолг.
- **Где работают агенты:** в первую очередь **Cursor** (всё можно поднять из Cursor, VS Code не обязателен). Визуализация «офис с персонажами» — расширение [Pixel Agents](https://github.com/pablodelucca/pixel-agents) для VS Code + Claude Code; в Cursor может быть недоступно. Подробно: **docs/PIXEL_AGENTS_AND_TASKS.md**.

---

## Расположение скиллов

Все скиллы в `.cursor/skills/`:

| Скилл | Назначение |
|--------|------------|
| `ru-compliance-context` | Общий контекст импортозамещения и норм (не ЗоКИИ); подключать ко всем ролям. |
| `developer` | Разработчик. |
| `tester` | Тестировщик (авто + ручные). |
| `infosec` | Специалист по информ. безопасности. |
| `deploy-tasktime` | Деплой TaskTime на VPS и целевые окружения. |
| `tasktime-workflow` | Практический пайплайн разработки и деплоя для MVP. |

При вызове агента указывать роль или скилл; для решений по архитектуре, разработке, ИБ, инфраструктуре — подключать **`ru-compliance-context`** вместе с ролью.

---

## Маршрутизация моделей по ролям

Для экономии токенов каждая роль имеет **модель по умолчанию**. Подробные правила: `.cursor/rules/token-economy.mdc`.

| Роль | Модель по умолчанию | Когда эскалировать на Opus |
|------|---------------------|---------------------------|
| Корпоративный архитектор | **Opus** | Всегда Opus (архитектурные решения) |
| Разработчик | **Sonnet** | Многомодульный рефакторинг, незнакомые паттерны |
| Тестировщик | **Sonnet** | e2e с моками внешних сервисов |
| Системный аналитик | **Sonnet** | Сложные интеграционные use-case |
| Специалист по ИБ | **Opus** | Всегда Opus для ИБ-согласования |
| Сетевик / инфраструктура | **Sonnet** | Новая инфра, миграция серверов |
| Deploy TaskTime | **Haiku** | — |
| Документация TaskTime | **Haiku** | — |
| Workflow (pipeline) | **Sonnet** | — |
| TaskTime Inbox | **Haiku** | — |

**Целевое соотношение:** 70% Sonnet, 20% Haiku, 10% Opus.

---

## Оценка ветки чата (деплой)

В ветке решались задачи:

- Установка twc-cli (pipx, PATH).
- Блок user-data / cloud-init при создании ВМ в Timeweb.
- Копирование setup-script на сервер (scp с Mac).
- Запуск setup-script на Ubuntu, исправление путей PostgreSQL (15 → 16).
- Применение схемы БД (cat \| psql), seed из backend с .env из app.
- Документация: DEPLOY.md, DEPLOYMENT_STEPS.md, ACCOUNTS.md; учётки команды; workflow (согласовать → залить).

Отсюда выделены скиллы `deploy-tasktime`, `docs-tasktime`, `tasktime-workflow`.
